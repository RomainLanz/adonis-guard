'use strict'

class {{name}} {
  index (user) {
    //
  }

  show (user, {{resource}}) {
    //
  }

  create (user) {
    //
  }

  update (user, {{resource}}) {
    //
  }

  create (edit) {
    //
  }

  destroy (user, {{resource}}) {
    //
  }
}

module.exports = {{name}}
